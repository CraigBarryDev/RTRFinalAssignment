#ifndef UTILS_H
#define UTILS_H

#include "GLHeaders.h"

static int windowWidth = 1000;
static int windowHeight = 800;
static float projFOV = 45.0f;
static float projZNear = 0.1f;
static float projZFar = 100.0f;

static glm::mat4 projMatrix(1.0f);

#endif
